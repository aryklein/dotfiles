# General configuration
general {
    gaps_in = 5
    gaps_out = 5
    border_size = 2
    #col.active_border = rgba(33ccffee) rgba(00ff99ee) 45deg
    #col.active_border = rgba(78a9ffee) rgba(46c880ee) 45deg
    col.active_border = $teal $blue 45deg
    col.inactive_border = $surface2

    layout = dwindle
}

decoration {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more
    rounding = 0
    shadow:enabled = true
    shadow:range = 5
    blur {
      enabled = no
    }
}

animations {
    enabled = yes
    # Some default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more
    bezier = myBezier, 0.05, 0.9, 0.1, 1.05

    animation = windows, 1, 7, myBezier
    animation = windowsOut, 1, 7, default, popin 80%
    animation = border, 1, 10, default
    animation = borderangle, 1, 8, default
    animation = fade, 0, 7, default
    animation = workspaces, 1, 7, default, fade
}

dwindle {
    preserve_split = yes # you probably want this
    force_split = 2
    # Split orientation: vertical (side-by-side) when W/H > split_width_multiplier, else horizontal.
    # Raise the multiplier on widescreens where W > H persists after several splits.
    # DP-1 2560x1080 step3 (right half 1280x1080, W/H ~= 1.185): multiplier >= 1.185 -> horizontal.
    # 1.3 chosen empirically (gaps/force_split shift the exact threshold from the raw ratio).
    split_width_multiplier = 1.3
}

master {
    new_on_top = true
    mfact = 0.5
}

gestures {
    workspace = off
}

misc {
    disable_hyprland_logo = yes
}

binds {
    workspace_back_and_forth = yes
    allow_workspace_cycles = yes
}
